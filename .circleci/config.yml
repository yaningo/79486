version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor:
      name: win/default
      shell: powershell.exe
    steps:
      - run: systeminfo
      - checkout
      - run:
          command: |
              mkdir C:\BuildTools
              cp Dockerfile C:\BuildTools              
              
      - run:
          #shell: cmd.exe
          command: |
              cd C:\BuildTools
              docker build -t buildtools2017:15.9.28307.960 -t buildtools2017:latest -m 4GB .
